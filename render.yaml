services:
  - type: web
    name: youtube-download-server
    env: node
    buildCommand: npm install && apt-get update && apt-get install -y python3 ffmpeg && pip3 install yt-dlp
    startCommand: node server.js
    plan: free
    healthCheckPath: /health
